# Current test prompt

Working prompt for whatever sticker is being iterated right now — gets overwritten each round instead of pasted in chat. Once locked, it moves into `shonen-pack.md` and this file resets for the next sticker.

(empty — sticker #5 locked, ready for the next sticker)
